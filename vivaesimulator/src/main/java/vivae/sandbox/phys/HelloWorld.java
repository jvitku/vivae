package vivae.sandbox.phys;
//Import all Phys2D libraries
/*
import phys2d.bodies.*;
import phys2d.joints.*;
import shapes.*;
import phys2d.raw.collide.*;
import phys2d.phys2d.raw.strategies.*;
import phys2d.phys2d.raw.forcesource.*;
import phys2d.phys2d.util.*;
import phys2d.phys2d.raw.shapes.*;
*/
import net.phys2d.*;
import net.phys2d.raw.collide.*;
import net.phys2d.raw.strategies.*;
import net.phys2d.raw.World;


public class HelloWorld {

/*

	// see: http://pphys2d.jmcouillard.com/pages/view/info

	//Create a PPhys2D world
	PPWorld world = new PPWorld();

	//Create a PPhys2D Box
	PPBox myBox;

	void setup () {
	  
	  //Set size and framerate
	  frameRate(30);
	  size(500,500);

	  //Set a standard world gravity
	  world.setGravity(0, 200);
	  
	  //Set world edges in dark gray
	  world.setEdges(this, new Color (40, 40, 40));
	  
	  //Add a box to the world
	  myBox = new PPBox(50, 90);
	  myBox.setPosition(250, 100);
	  myBox.setStrokeWidth(1);
	  myBox.setRotation(0.2);
	  myBox.setFillColor(new Color(40,40,100));
	  myBox.setStrokeColor(new Color(40,40,40));
	  world.add(myBox);

	}

	void draw () {
	  //Clear screen
	  background(255); 
	  //Draw world
	  world.draw(this);
	}
	*/
}
